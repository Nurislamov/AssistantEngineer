# EquipmentDiagnostics Manual Source Registry

This non-runtime registry identifies local manual intake sources used by staging candidates.

- Registry JSON may be committed.
- PDF files remain under ignored `artifacts/manual-intake/sources/` and must not be committed.
- A registry entry is not diagnostic knowledge and is never loaded into the runtime catalog.
- Manual-backed candidates remain staging-only until a reviewed runtime promotion PR.
